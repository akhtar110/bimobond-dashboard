/// Presentation-only filters and sort — does not change API contracts.
enum CategorySortOption {
  name,
  mostSubcategories,
  recentlyCreated,
  recentlyUpdated,
}

enum CategoryHasChildrenFilter {
  all,
  yes,
  no,
}
