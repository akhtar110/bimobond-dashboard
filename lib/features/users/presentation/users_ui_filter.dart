/// UI filter enum used by [UsersBloc] and the users presentation layer.
enum UsersUiFilter { all, verified, banned, online, offline }

/// Presence filter enum for online / offline presence.
enum UsersPresenceFilter { all, online, offline }
